# =============================================================================
# SAFE DEFAULTS - This file is committed to git
# For secrets and local overrides, create .env.local (git-ignored)
# =============================================================================

# Database Configuration
# Docker Compose defaults (container networking)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=calsync
DATABASE_URL=postgresql://postgres:postgres@db:5432/calsync

# =============================================================================
# Docker Deployment Port Configuration
# =============================================================================
# IMPORTANT: To change the exposed port, update ALL THREE values below:
# 1. EXTERNAL_PORT - Used by docker-compose.yml
# 2. API_URL - Must match EXTERNAL_PORT
# 3. FRONTEND_URL - Must match EXTERNAL_PORT
# Don't forget to update Google OAuth redirect URI too!
# =============================================================================
EXTERNAL_PORT=8033
API_URL=http://localhost:8033
FRONTEND_URL=http://localhost:8033

# Environment
ENVIRONMENT=development
DEBUG=true

# Sync settings
SYNC_LOOKAHEAD_DAYS=90

# =============================================================================
# SECRETS - Create .env.local with these values (see .env.example)
# =============================================================================
# OAUTH_CLIENT_ID=your_google_oauth_client_id
# OAUTH_CLIENT_SECRET=your_google_oauth_client_secret
# JWT_SECRET=generate_with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# ENCRYPTION_KEY=generate_with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
