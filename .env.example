# =============================================================================
# ENVIRONMENT CONFIGURATION GUIDE
# =============================================================================
# This repository uses a two-file approach for environment configuration:
#
# 1. .env (committed to git)
#    - Safe defaults for Docker deployment
#    - No secrets, safe to commit
#    - Used by docker-compose.yml
#
# 2. .env.local (git-ignored, YOU create this)
#    - Your secrets and local overrides
#    - NEVER commit this file
#    - Overrides values from .env
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your secrets below (OAuth, JWT, Encryption)
# 3. Optionally override any .env defaults for local development
# =============================================================================

# =============================================================================
# SECRETS (Required - add to .env.local)
# =============================================================================

# Google OAuth (Web Application)
# Get these from: https://console.cloud.google.com/apis/credentials
OAUTH_CLIENT_ID=your_oauth_client_id_here
OAUTH_CLIENT_SECRET=your_oauth_client_secret_here

# Security Keys (Generate new ones!)
# JWT_SECRET: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# ENCRYPTION_KEY: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
JWT_SECRET=your_jwt_secret_here
ENCRYPTION_KEY=your_encryption_key_here

# =============================================================================
# OPTIONAL OVERRIDES (add to .env.local if needed)
# =============================================================================

# Docker Image (for production deployment)
# Uncomment in .env.local to use registry image instead of local build
# DOCKER_IMAGE=ghcr.io/yourusername/cal-sync:latest

# =============================================================================
# DOCKER PORT CONFIGURATION (add to .env.local to override)
# =============================================================================
# To change the exposed Docker port from default 8033:
# 1. Set EXTERNAL_PORT to your desired port (e.g., 8088)
# 2. Update API_URL and FRONTEND_URL to match
# 3. Update Google OAuth redirect URI: http://localhost:PORT/api/oauth/callback
# 4. Restart containers: docker compose down && docker compose up -d
#
# Example for port 8088:
# EXTERNAL_PORT=8088
# API_URL=http://localhost:8088
# FRONTEND_URL=http://localhost:8088

# Database (override for local development)
# Default in .env uses Docker container (db:5432)
# Uncomment in .env.local for local database:
# POSTGRES_PASSWORD=dev
# DATABASE_URL=postgresql://postgres:dev@localhost:5433/calsync

# API URLs (override for local development)
# Default in .env uses unified Docker deployment (port 8033)
# For local development with separate backend (port 8000) and frontend (port 3033):
# API_URL=http://localhost:8000
# FRONTEND_URL=http://localhost:3033

# Environment
# Defaults in .env: ENVIRONMENT=development, DEBUG=true
# For production, set in .env.local:
# ENVIRONMENT=production
# DEBUG=false

# Sync settings
# Default in .env: SYNC_LOOKAHEAD_DAYS=90
# SYNC_LOOKAHEAD_DAYS=120
